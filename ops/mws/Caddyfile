# HTTPS-терминация: Caddy сам выпускает и продлевает Let's Encrypt.
# DOMAIN приходит из .env через docker-compose.
{$DOMAIN} {
	encode gzip
	reverse_proxy app:8000
}

www.{$DOMAIN} {
	redir https://{$DOMAIN}{uri} permanent
}
