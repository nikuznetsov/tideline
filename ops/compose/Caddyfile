# HTTPS termination: Caddy issues and renews Let's Encrypt certificates on its own.
# DOMAIN comes from .env via docker-compose.
{$DOMAIN} {
	encode gzip
	reverse_proxy app:8000
}

www.{$DOMAIN} {
	redir https://{$DOMAIN}{uri} permanent
}
